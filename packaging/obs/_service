<services>
  <!-- Mirrors the working pattern from home:ciriarte:network-tools: tar_scm
       emits a .tar directly, set_version patches the spec/.dsc/changelog
       version, recompress gzips it. Go modules are vendored in-tree (vendor/),
       so the build is offline without a go_modules service. -->
  <service name="tar_scm">
    <param name="scm">git</param>
    <param name="url">https://github.com/ciroiriarte/pve-cli</param>
    <param name="version">0.11.2</param>
    <param name="revision">v0.11.2</param>
    <param name="filename">pve-cli</param>
  </service>
  <service name="set_version">
    <param name="basename">pve-cli</param>
  </service>
  <service name="recompress">
    <param name="file">*.tar</param>
    <param name="compression">gz</param>
  </service>
</services>
