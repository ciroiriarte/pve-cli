# Build output
/pc
/dist/
*.test
*.out

# Go
# NOTE: vendor/ IS committed — distro packaging (OBS) builds offline from it.

# Editor / OS
.DS_Store
*.swp

# Local secrets / config (never commit)
*.local.yaml

# Agent / tooling state (not project source)
.omc/
.claude/
gendocs-out/
